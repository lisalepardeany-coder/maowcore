// SPA mode: no server-side rendering, client-side routing only.
// adapter-static emits a fallback index.html that boots the app on any route.
export const ssr = false;
export const prerender = false;
export const trailingSlash = 'ignore';
